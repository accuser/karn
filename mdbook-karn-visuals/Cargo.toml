[package]
name = "mdbook-karn-visuals"
version.workspace = true
edition.workspace = true
# Internal docs-build tooling — never published to crates.io.
publish = false

[[bin]]
name = "mdbook-karn-visuals"
path = "src/main.rs"

[dependencies]
serde_json.workspace = true
