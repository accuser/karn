context demo

type UserId = String where NonEmpty

actor User { auth = Bearer(secret = "S"), identity = UserId }
actor Admin = User where isAdmin(user)

service api from http {
  on GET("/x") by a: Admin () -> Effect[HttpResult[String]] {
    Ok("x")
  }
}
