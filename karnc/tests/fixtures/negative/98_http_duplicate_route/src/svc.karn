context svc

service api from http {
  on GET("/orders/:id") (id: String) -> Effect[HttpResult[String]] {
    Ok("first")
  }

  on GET("/orders/:id") (id: String) -> Effect[HttpResult[String]] {
    Ok("second")
  }
}
