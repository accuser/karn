context jobs

type Job = {
  id: String,
}

service worker {
  on queue "high-priority" (message: Job) -> Effect[Result[(), String]] {
    Ok(())
  }

  on queue "low-priority" (message: Job) -> Effect[Result[(), String]] {
    Ok(())
  }
}
