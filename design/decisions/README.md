# Decision records

The design decisions that shaped Bynk's incremental evolution, one record per
language-defining call: `Status / Context / Decision / Consequences`, each
citing the [normative spec](../../docs/src/spec/index.md) section that now
embodies it. Records were harvested from the retired per-increment instalment
documents (see the spec's Appendix B); from v0.19 on, each increment adds its
records here as part of the increment itself.

A record is **immutable once accepted** — a reversal is a new record that
supersedes the old one, the way 0016 supersedes the interim portability tiers.

The table below is **complete by construction**: a CI drift guard (`bynkc`'s
`decisions_index` test, run by the `drift` job) fails if an ADR file has no row
or a row links to no file. Summaries and statuses are curated by hand; the
*presence* of every record is enforced.

| # | Decision | Status |
|---|---|---|
| [0001](0001-literal-admission-set.md) | Compile-time literal admission is a narrow, closed set | Accepted (v0.9.4) |
| [0002](0002-cron-queue-slicing.md) | v0.10 ships as two slices; queues are consumer-only | Accepted (v0.10) |
| [0003](0003-inline-state-initialisers.md) | Inline state-field initialisers, not an `init` block | Accepted (v0.11) |
| [0004](0004-static-initialiser-expressions.md) | State initialisers are a closed static-expression set | Accepted (v0.11) |
| [0005](0005-provider-constructor-injection.md) | Provider composition is constructor injection in topo order | Accepted (v0.12) |
| [0006](0006-provider-cycles-rejected.md) | Provider dependency cycles are rejected | Accepted (v0.12) |
| [0007](0007-is-refinement-narrowing.md) | Refinement narrowing reuses `is`, disambiguated at check time | Accepted (v0.13) |
| [0008](0008-cross-context-capabilities-local.md) | Cross-context capabilities wire by local instantiation | Accepted (v0.15) |
| [0009](0009-integration-tests-simulated-wire.md) | Integration tests run a simulated wire in Node | Accepted (v0.16) |
| [0010](0010-adapter-unit-kind.md) | Adapters are a distinct unit kind with no logic | Accepted (v0.17) |
| [0011](0011-consumes-capability-selection.md) | Braced `consumes` flattens capabilities; clashes rejected | Accepted (v0.17) |
| [0012](0012-reserved-bynk-surface.md) | The `bynk` surface is reserved, flat, ambient-only | Accepted (v0.17/v0.18) |
| [0013](0013-explicit-binding-clause.md) | Explicit `binding` clause; pinned npm deps declared there | Accepted (v0.17) |
| [0014](0014-refined-ids-privileged-constructor.md) | Refined boundary IDs; bindings construct through `.of` | Accepted (v0.17) |
| [0015](0015-canonical-provider-symbols.md) | The `bynk` surface names canonical provider symbols | Accepted (v0.17) |
| [0016](0016-no-portable-infrastructure.md) | No portable infrastructure tier | Accepted (v0.17) |
| [0017](0017-platform-lock-per-deployment-unit.md) | Platform lock is per deployment unit | Accepted (v0.17 design) |
| [0018](0018-config-as-capability.md) | Config and IO are capabilities; no `needs` clause | Accepted (v0.17/v0.18) |
| [0019](0019-adapter-dependencies.md) | Adapter-to-adapter dependencies via braced `consumes` + `given` | Accepted (v0.18) |
| [0020](0020-adapter-dependency-trust.md) | Adapter npm-dependency trust policy | **Open** |
| [0021](0021-secrets-env-source.md) | Secrets: optional injected env + `globalThis` probe | Accepted (v0.18) |
| [0022](0022-fetch-minimal-typed-core.md) | Fetch ships a minimal typed core pending sequence types | Accepted (v0.18) |
| [0023](0023-platform-adapter-before-stdlib.md) | The Cloudflare adapter lands before the standard library | Accepted (post-v0.18) |
| [0024](0024-platform-native-via-first-party-metadata.md) | Platform-native marking is first-party metadata, not syntax | Accepted (v0.19) |
| [0025](0025-env-threading-for-platform-resources.md) | Platform resources reach bindings via threaded env, both targets | Accepted (v0.19) |
| [0026](0026-platform-adapters-under-bynk-prefix.md) | Platform adapters live inside the reserved `bynk.*` prefix | Accepted (v0.19) |
| [0027](0027-lambda-value-syntax.md) | Lambda syntax is `(params) => expr`, the shared value arrow | Accepted (v0.20a) |
| [0028](0028-open-narrow-generics.md) | Generics are Open-narrow: functions only, no bounds | Accepted (v0.20a) |
| [0029](0029-type-argument-inference.md) | Type arguments: argument-directed inference + explicit fallback | Accepted (v0.20a) |
| [0030](0030-function-types-non-boundary.md) | Function types are confined to non-boundary positions | Accepted (v0.20a) |
| [0031](0031-effect-non-storable.md) | `Effect[T]` stays non-storable; `<-` confinement extends to function values | Accepted (v0.20a) |
| [0032](0032-named-functions-as-values.md) | Named functions are values where a function type is expected | Accepted (v0.20a) |
| [0033](0033-closures-over-capabilities.md) | Closures over capabilities; bottom-up lambda effectfulness | Accepted (v0.20a) |
| [0034](0034-collections-hybrid-kernel-stdlib.md) | Collections: thin built-in kernel, Bynk-written combinator stdlib | Accepted (v0.20b) |
| [0035](0035-list-map-builtin-immutable.md) | `List`/`Map` built-in, immutable; lowerings, wire format, order | Accepted (v0.20b) |
| [0036](0036-collection-kernel-ops.md) | The collection kernel: `prepend` builder, `fold` + `foldEff` iteration | Accepted (v0.20b) |
| [0037](0037-collection-call-surface.md) | Collection call surface: built-in methods, statics, free combinators | Accepted (v0.20b) |
| [0038](0038-map-value-keyable-keys.md) | `Map` keys are value-keyable only | Accepted (v0.20b) |
| [0039](0039-list-literal-empty-inference.md) | List literal syntax; empty-literal inference; the line rule for `[` | Accepted (v0.20b) |
| [0040](0040-float-distinct-erased-base-type.md) | `Float` is a distinct base type, erased to `number`; finite at the boundary | Accepted (v0.21) |
| [0041](0041-no-numeric-coercion-named-conversions.md) | No implicit `Int`↔`Float` coercion; conversions are value methods | Accepted (v0.21) |
| [0042](0042-operand-typed-division.md) | Operand-typed division; non-finite arithmetic is host-defined | Accepted (v0.21) |
| [0043](0043-float-literals.md) | Float literals: fraction/exponent, digit-both-sides, reject overflow, store the lexeme | Accepted (v0.21) |
| [0044](0044-refinement-over-float.md) | Refinement over `Float`: float bounds, numeric predicates extend, bounds match the base | Accepted (v0.21) |
| [0045](0045-typed-json-codec.md) | The typed JSON codec: compiler-backed, no untyped `Json`; type-app on statics | Accepted (v0.22b) |
| [0046](0046-string-kernel.md) | The string kernel: built-in methods, UTF-16 code units, pinned footguns | Accepted (v0.22a) |
| [0047](0047-jsonerror-compiler-known.md) | `JsonError`: a compiler-known, Bynk-inspectable record | Accepted (v0.22b) |
| [0048](0048-combinators-as-kernel-methods.md) | `Option`/`Result` combinators and numeric helpers are kernel methods | Accepted (v0.22a) |
| [0049](0049-bare-int-boundary-integrality.md) | Bare-`Int` boundary fields validate integrality (wire-contract tightening) | Accepted (v0.22b) |
| [0050](0050-kv-list-drain.md) | `Kv.list` is a binding-side drain; cursor-paging deferred (the `given`-gap) | Accepted (v0.23) |
| [0051](0051-kv-write-options-as-ops.md) | `Kv` write options as distinct camelCase ops (`putTtl`) | Accepted (v0.23) |
| [0052](0052-lsp-project-diagnostics.md) | LSP project-wide diagnostics: non-bailing, overlay-aware, file-attributed | Accepted (v0.24) |
| [0053](0053-lsp-binding-index.md) | The LSP binding index: a reference-table sink at the resolution sites; rename validated by re-analysis + index equality | Accepted (v0.25) |
| [0054](0054-structured-suggestions.md) | Quick-fixes are structured suggestions authored at the diagnosis site; the LSP keys on the diagnostic's span, serves from the cached round | Accepted (v0.26) |
| [0055](0055-index-riders.md) | `workspace/symbol` + `documentHighlight` as `ProjectIndex` queries (`kind` omitted) | Accepted (v0.26) |
| [0056](0056-lsp-inlay-hints.md) | Inlay hints: a curated set harvested via a checker sink (`let`/`let <-`/lambda params; peeled payload; survives errors at reached sites) | Accepted (v0.27) |
| [0057](0057-lsp-semantic-tokens.md) | Semantic tokens read the index over a frozen legend; first-party references via a tokens-only `foreign_refs` side table; `refined` only with a refinement present | Accepted (v0.28) |
| [0058](0058-extension-mirrors-legend.md) | The VS Code extension declares + themes the legend's custom tokens; the names are a cross-component contract enforced by a `bynk-lsp` test parsing `package.json` (excluded from the published tarball) | Accepted (v0.29) |
| [0059](0059-refactor-track-posture.md) | The refactor track: behaviour-preserving, patch-versioned, trunk-based, golden-verified | Accepted (v0.29.1) |
| [0060](0060-named-concern-modules.md) | Split sprawling files into named single-concern modules (flat re-export is fine) | Accepted (v0.29) |
| [0061](0061-completion-sliced-positional-first.md) | Completion is sliced; positional first, lexical contexts, static built-ins | Accepted (v0.30) |
| [0062](0062-completion-member-name-vs-value-receiver.md) | `.`-member completion splits by receiver: name now, typed value later | Accepted (v0.30.1) |
| [0063](0063-completion-value-receiver-typing.md) | Value-receiver `.`-member completion: retained `expr_types`, enumerable kernels | Accepted (v0.30.2) |
| [0064](0064-locals-in-scope.md) | Locals in scope: scope ranges from the checker, resolved per-file | Accepted (v0.31) |
| [0065](0065-signature-help.md) | Signature help: lexical call context, name-vs-value callees, shared renderer | Accepted (v0.32) |
| [0066](0066-codelens-reference-counts.md) | CodeLens reference counts from the index | Accepted (v0.33) |
| [0067](0067-call-hierarchy.md) | Call hierarchy from a preserved-owner call graph | Accepted (v0.34) |
| [0068](0068-implementation-navigation.md) | Implementation navigation from a provides-clause impl graph | Accepted (v0.35) |
| [0069](0069-member-index-kinds.md) | Member index kinds via compound names | Accepted (v0.36) |
| [0070](0070-folding-selection-ranges.md) | Folding & selection ranges from one recovered-AST visitor | Accepted (v0.37) |
| [0071](0071-extension-polish.md) | B-2 extension polish: scaffolds, commands, walkthrough, problem-matcher | Accepted (v0.38) |
| [0072](0072-richer-inlay-hints.md) | Richer inlay hints: parameter names + generic instantiations | Accepted (v0.39) |
| [0073](0073-inrange-swap-quickfix.md) | InRange-swap quick-fix via per-bound spans | Accepted (v0.40) |
| [0074](0074-numeric-tostring.md) | `toString` on the numeric kernel | Accepted (v0.42) |
| [0075](0075-string-interpolation.md) | String interpolation (`\(expr)` holes) | Accepted (v0.43) |
| [0076](0076-feature-track-posture.md) | Far-reaching features run as a feature track: persistent design doc, settle-first, front-loaded ADRs, per-slice proposals | Accepted (posture) |
| [0077](0077-service-protocol-on-header.md) | Service protocol moves to the header (`from <protocol>`); method-builders; `from`-less ⇒ `call`-only | Accepted (v0.44) |
| [0078](0078-queueresult-typed-verdict.md) | `QueueResult` typed queue verdict (`Ack`/`Retry`); the agency rule; cron keeps `Result[(), E]` | Accepted (v0.44) |
| [0079](0079-protocols-closed-set.md) | Protocols are a closed set (transports are open); `protocol` reserved; the protocol-descriptor seam | Accepted (v0.44) |
| [0080](0080-actor-schemes-closed-nominal.md) | Auth schemes are a closed, compiler-known nominal set; actors are nominal contracts (sealed-now-openable) | Accepted (v0.45) |
| [0081](0081-verified-identity-context-sealed.md) | A verified actor identity is a context-sealed value: minted at the seam, threaded service→agent, never re-checked | Accepted (v0.45) |
| [0082](0082-by-clause-verify-then-body-defaults.md) | The `by` clause; two-phase verify-then-body; silent per-protocol default actors; HTTP requires `by` | Accepted (v0.45) |
| [0083](0083-bynk-driver-thin-orchestrator.md) | Introduce the `bynk` driver as a thin orchestrator distinct from `bynkc`; resolution order (override → PATH → sibling); driver↔compiler skew; portable detection | Accepted (v0.46) |
| [0084](0084-doctor-output-exit-contract.md) | The `bynk doctor` output / exit-code contract: capability-grouped checks; bare informational, `--only` gates, `--strict` escalates; `--format short`/`json` pinned | Accepted (v0.46) |
| [0085](0085-bearer-token-jwt-hs256.md) | BearerToken is compiler-generated JWT/HS256; identity is the `sub` claim through a string-constructible identity type; HTTP-only; fail-closed → 401 | Accepted (v0.47) |
| [0086](0086-firstparty-sources-as-files-vendored.md) | First-party sources authored as files (embedded via `include_str!`), vendored not published while part of the emit ABI | Accepted (v0.48) |
| [0087](0087-security-ci-posture.md) | Emitted security boundaries carry a standing behavioral bypass-class test; CodeQL SAST + npm audit committed; native secret-scanning push protection | Accepted (v0.49) |
| [0088](0088-optional-by-binder.md) | The `by` binder is optional (`by <Actor>` for anonymous / verify-and-discard); amends 0082; `_` not admitted as a binder | Accepted (v0.50) |
| [0089](0089-signature-hmac-sha256-webhooks.md) | Signature is compiler-generated HMAC-SHA256 over the raw body; configurable header; timestamp-tolerance replay window; HTTP-only, body-required, identity `()` | Accepted (v0.51) |
| [0090](0090-multi-actor-sum-dispatch.md) | Multi-actor handlers are an ordered sum of peer actors, resolved first-wins, keyed by scheme; a sum requires a binder; the body matches the resolved nominal actor; HTTP-only; total failure → 401 | Accepted (v0.52) |
| [0091](0091-authorisation-invariants-refinement-actors.md) | Authorisation invariants are refinement actors over a Bearer base; a closed claim-predicate set checked against the verified claims at the boundary; a failed invariant is 403, distinct from 401; an `Admin` is-a `User` | Accepted (v0.53) |
| [0092](0092-cross-context-caller-value.md) | The cross-context `CallerId` value is the calling context's name, stamped into an `X-Bynk-Caller` header beside the args body, read at the callee boundary and threaded into `deps`; absent caller is fail-closed; static / channel trust, first-party | Accepted (v0.54) |
| [0093](0093-completion-surface-contract.md) | Completion has one canonical *context × candidate-kind* matrix; every populated cell is registry-sourced and complete (coverage-tested); the clean-file ceiling is confined to the value-receiver cell; `.` is a trigger character | Accepted (doc-ADR) |
| [0094](0094-error-tolerant-receiver-typing.md) | Lift the clean-file ceiling by recording best-effort partial `expr_types` in Analyse mode (the checker already computes them; the final `errors.is_empty()` gate withheld them); Build stays Ok-only, so codegen is untouched; monotonic — never worsens | Accepted (slice 4) |
| [0095](0095-unit-source-map.md) | The project analysis exposes `unit_sources` (qualified unit name → project source files), built from non-synthetic parsed files on the structurally-analysed path; the shared enabler for document links and consumed-context navigation that the binding index can't serve | Accepted (slice 6b) |
| [0096](0096-bynk-dev.md) | `bynk dev` builds + serves locally in one step: compile to a managed, gitignored `.bynk/dev/` (cleared each build so stale contexts can't trip selection); select-or-default the context's worker; serve via `wrangler dev` in local mode — no provisioning (the `<KV_NAMESPACE_ID>` placeholder is verified-ignored locally), no `--remote`, `--` passthrough for wrangler flags | Accepted (v0.57) |
| [0097](0097-bynk-new.md) | `bynk new <path>` scaffolds a complete, runnable single-context HTTP service (`bynk.toml` + `.gitignore` + `src/<name>.bynk`) that `bynk dev` serves unmodified — closing the `doctor → new → dev` arc; offline, no-toolchain `std::fs` file-writing; embedded compile-tested template (ADR 0086 precedent); name validated by the real lexer with a `--name` fix-it; refuse-non-empty (ignoring VCS/OS cruft); `.gitignore` is just `/.bynk` | Accepted (v0.58) |
| [0098](0098-bynkc-test-json.md) | `bynkc test --format json` emits a pinned `#[derive(Serialize)]` document (declaration-order = contract) folded from the generated runner's NDJSON (internal protocol, not a consumer surface); three terminal states on a closed `error.kind` (`compile` carries the `$bynkc` diagnostic lines; `runtime` carries a crashed runner's stderr); exit code follows the runner's process status. The `assert` lowering now emits a real project-relative `path:line:col` (was a byte offset), for unit + integration uniformly. A VS Code Test Explorer shells it with click-through; no `bynk-test` crate (a future `bynk test` shells `bynkc`) | Accepted (v0.59) |
| [0099](0099-crate-layering-dependency-direction.md) | `bynkc` decomposes **downward** into a layered, published library set (`bynk-syntax`/`-check`/`-emit`/`-ide`/`-render`); every dependency arrow points down, no upward façades; the five new crates are published lockstep (`version.workspace = true`) | Accepted (crate-decomposition track, slice 0) |
| [0100](0100-structured-data-rendering-separation.md) | Libraries emit structured data and never render; a single shared `bynk-render` crate (over `bynk-syntax::CompileError` **only**) renders for both CLIs identically by construction; the `AttributedError → CompileError` flattening stays above render (no `render → emit` edge); the LSP maps to protocol | Accepted (crate-decomposition track, slice 0) |
| [0101](0101-front-end-links-pipeline-binary-topology.md) | The front-end **links** the library set (no longer shells `bynkc`); a thin `bynkc` binary survives for CI/build determinism + `cargo install bynkc`, `bynk` becomes the human front-end; amends 0083; re-mechanises the deferred v0.59 `bynk test` from shell to link | Accepted (crate-decomposition track, slice 0) |
| [0102](0102-foundation-types-boundary.md) | `span` / `CompileError` / source-cache / the `diagnostics` code registry live in the lowest leaf `bynk-syntax` (keeps the graph acyclic); the now-cross-crate drift tests get fixed homes — `diagnostics_registry` as a workspace integration test, `kernel_registry` in `bynk-ide` dev-depending on `bynk-check` | Accepted (crate-decomposition track, slice 0) |
| [0103](0103-source-map-contract.md) | Source maps are line-level, statement-anchored, emitted as siblings (`.ts.map` + `//# sourceMappingURL`) by `bynk-emit`; the lowering-gap rule anchors every generated line to its enclosing source statement (spike-ratified by stepping `?`/`match` under the V8 inspector — 8→3, 13→6 stops coalesce monotonically); `names` from preserved identifiers; runtime + glue are the skip boundary; emission is unconditional but `sourcesContent`/bundle-chaining are profile-gated so `.bynk` source never reaches a deployed Worker by default | Accepted (debugging track, slice 0) |
| [0104](0104-debug-launch-model.md) | Reuse VS Code's JavaScript debugger via a thin `DebugConfigurationProvider` (compile → start under inspector → hand off); no bespoke Debug Adapter in phase 1 (the map does the relocation; the spike showed glue suffices); targets the Node test runner (`pwa-node`) and workerd (`wrangler dev` CDP, `bynk dev --inspect` fallback); `skipFiles` over toolchain machinery; remote/deployed debugging deferred | Accepted (debugging track, slice 0) |
| [0105](0105-semantic-debug-interposition.md) | The bynk-native semantic layer rewrites js-debug's DAP responses (`variables`/`scopes`/`stackTrace`) **editor-side** — runtime-agnostic, so it reaches workerd (unlike slice 5's in-debuggee generator). Mechanism: a `DebugAdapterTracker` mutates responses in `onDidSendMessage` (spike-confirmed despite the observe-only docs); the cheapest rung. Bounded by a pinned VS Code + a committed regression-guard spike + a rung-2 wrapping-proxy fallback; bespoke DAP-over-CDP stays out. Inference-first (tag/`__`-temps/`deps`), emitter metadata per-slice; binds only to `__bynkChild` sessions, gated by `bynk.debug.semanticValues` | Accepted (semantic-debugging track, slice 0) |
| [0106](0106-async-message-send.md) | Asynchronous message send `~>`: the **caller** chooses sync-vs-async (no `call`/`cast` keywords, no `Oneway` type — the operation only publishes its `-> Effect[...]` reply contract); an **error gate** restricts `~>` to `Effect[()]` (`bynk.send.requires_unit`/`non_effect`/`in_pure_context`) since a fire-and-forget send drops its reply; the marker is a leading **`~>` glyph** (not `<-`, not a `send` keyword that would clash with `Fetch.send`), LL(1) at statement head. Emits `deps.__exec.waitUntil(e)` (immediate tier, threaded only into sending contexts); the at-commit tier + migration defer to Events | Accepted (v0.79) |
| [0107](0107-agent-invariant-predicate-surface.md) | Agent invariants (§14): a **closed, agent-local predicate surface** (state fields by bare name, base operators, `implies`, `is`, a closed set of pure value methods — no capabilities/effects/cross-agent refs); `implies` is a reserved lowest-precedence operator desugaring to `!P \|\| Q`; invariants form a phase between `state { }` and the handlers. A **dedicated `InvariantViolation` fault** (not a `BoundaryError`) thrown in `commitState` before the `put` — "revert" means **non-persistence of the offending commit**, not whole-handler rollback. Runtime-checked MVP; static provable-violation pass and a general typed-agent-fault channel are named follow-ons | Accepted (v0.80) |
| [0108](0108-state-record-to-store-fields.md) | Agent state becomes `store` fields (§10): `store` **replaces** the `state { }` record (one canonical surface, §2) rather than coexisting; removal is a **hard cutover with a `bynk-fmt` codemod**, not a deprecation window; transitional dual-surface across slices ends at the parity slice; "agent state" survives as the *concept* (committed aggregate, invariants, white-box reads). Carries ADRs 0003/0004 forward onto `store` fields; restates ADR 0107's bare-name/commit model; depends on ADR 0109 (handler-atomic commit) | Accepted (storage track) |
| [0109](0109-handler-atomic-commit.md) | The **handler is the atomic unit for storage** (§10/§12): every `store` write stages into a per-handler write-set and **flushes once at handler end** behind the DO output gate; reads consult overlay-then-storage (read-your-writes); invariants check the proposed state before the flush. **Amends ADR 0107 D5/D6** — for *state* the fault-reverts-nothing guarantee is restored (D6 reversed); the **effect-release split** rules per class: state atomic, event emission at-commit (contract for the Events track), `~>` immediate-and-standing (ADR 0106), cross-agent calls standing (sagas, §13). Explicit `commit` keyword retired (implicit handler-end commit); atomicity is **intra-agent and intra-invocation** (atomic ≠ exactly-once; cross-invocation is the Idempotency capability) | Accepted (storage track) |
| [0110](0110-storage-map-vs-value-map.md) | `Map` is **one type, two op sets**, disambiguated by **receiver provenance** (rejecting split names): a `store` field of `Map[K,V]` is a *storage map* (effectful entry methods `put`/`get`/`update`/`upsert`/`remove`/`contains`/`size`, no `:=`; `update`-on-absent faults); a value is the immutable collection, unchanged. Persisted as a `Record<string,V>` field of the state record (**wholesale**, reusing the `Cell` commit; per-entry storage keys deferred). A keyed `map.get(k)` is admissible in invariants (ADR 0108 D5). `@indexed`, the query/iteration surface (§11), and per-entry keys are named follow-ons | Accepted (storage track, slice 2) |
| [0111](0111-storage-annotation-surface.md) | Storage **annotations** (§10 / track Q3): a **closed registry** of four — `@ttl`/`@retain`/`@indexed`/`@bounded` — spelled `@name(args)` in field-declaration position (after the kind, before `=`), unknown names rejected (no open dialect). Each is pinned to its kind(s) and **gated to the slice that implements it** (parse-everything / gate-the-unsupported, like the kinds); arguments are **compile-time constant literals**. `@ttl`/`@retain` take a **`Duration`**, a distinct primitive (not bare `Int` — ADR 0041) spelled `5.minutes`/`30.days` and lowering to `Int` millis (`Clock`'s unit), introduced as a **prerequisite slice before Cache**. `@ttl` eviction is **lazy, check-on-read** against the runtime clock (sketch; Cache slice finalises). Re-sequences slice 3 → annotation surface → `Duration` → `Cache` | Accepted (storage track, slice 3 settling) |
| [0112](0112-duration-primitive.md) | **`Duration`** is a fifth **base type** erased to `Int` **milliseconds** (the `Clock` unit), mirroring `Float` (ADR 0040). Literal `<int>.<unit>` over a closed unit set (`milliseconds`…`days`) — a parser recognition over the existing `IntLit . Ident` shape (no new token; tree-sitter unaffected). v1 operators: `Duration ± Duration`, `Duration * Int` (scalar), and `Duration` comparison; subtraction **unclamped** (may go negative). **One sanctioned `Int`↔`Duration` mix** — `Int + Duration -> Int` (and `-`) for timestamp math (`clock.now() + 5.minutes`), the deliberate exception to ADR 0041; every other mix stays `no_numeric_coercion`. Conversions explicit: `d.toMillis() -> Int`, `Duration.millis(n) -> Duration`. Codec: JSON number, integer-on-read. `Instant`, scalar division, ratio, unit statics deferred | Accepted (storage track, slice 3b; **D4 superseded by [ADR 0114](0114-instant-primitive.md)**) |
| [0113](0113-cache-ttl-eviction.md) | **`Cache[K, V]`** is a `Map` with per-entry TTL expiry — persisted as `Record<string, { v, exp }>` (value + absolute expiry millis), extending the `Map` representation (ADR 0110) and committing atomically (ADR 0109). **`@ttl(d: Duration)` is required** on a `Cache` (a Cache without a TTL is a `Map`; `bynk.store.cache_ttl_required`) and becomes the **first functional annotation** (ADR 0111 gate flipped). Eviction is **lazy, check-on-read** (`get`/`contains`/`size` skip expired entries, reaped at next commit). **Time comes from `given Clock`, not an ambient clock** (`bynk.store.cache_needs_clock`) — **superseding ADR 0111 D6's sketch**: routing eviction time through the capability makes the dependency visible at the signature and eviction testable. Per-`put` TTL override, alarm reaping, `@bounded` deferred | Accepted (storage track, slice 3c) |
| [0114](0114-instant-primitive.md) | **`Instant`** is a sixth **base type** for absolute time, erased to `Int` **epoch milliseconds** (mirroring `Float`/`Duration`). **No literal** — minted by `Clock.now()` (re-typed to `Effect[Instant]`) and `Instant.fromEpochMillis(n)`, derived by arithmetic. Operators: `Instant ± Duration -> Instant`, `Instant − Instant -> Duration`, `Instant` comparison (chronological, orderable); every `Instant`↔`Int` mix stays `no_numeric_coercion`. **Supersedes [ADR 0112](0112-duration-primitive.md) D4** — with `Instant` carrying timestamp math, the `Int + Duration -> Int` coercion is withdrawn, restoring ADR 0041 with no exception. Conversions explicit (`toEpochMillis`/`fromEpochMillis`); codec JSON number, integer-on-read; zero = epoch. `Cache` expiry becomes an internal `Instant` compare (surface unchanged) | Accepted (query-algebra track, slice 1b) |
| [0115](0115-query-model-lazy-eager-dispatch.md) | **`Query[T]`** is a first-class, **by-reference**, **non-storable / non-boundary** type (reusing the `Effect`/`Fn` posture, ADRs 0031/0030; not value-comparable). **Lazy/eager evaluation is decided by receiver provenance**, generalising [ADR 0110](0110-storage-map-vs-value-map.md) from *op set* to *evaluation strategy*: a chain rooted in a `store` field (`Map`/`Set`/`Log`) is **lazy** (builders -> `Query[T]`, terminals -> `Effect[T]`); a chain rooted in an in-memory value is **eager**. A terminal's result leaves the lazy domain (no re-lazification); the storage-read effect **folds into the agent's storage capability** (no new `given`); building a query is pure. **Agent-locality** is a structural consequence of non-boundary. Vocabulary and lowering are separate ADRs | Accepted (query-algebra track, slice 0 settling) |
| [0116](0116-query-vocabulary-and-ordering.md) | The **closed** query/collection vocabulary — builders (`filter`/`map`/`flatMap`/`sortBy`/`take`/`skip`/`distinct`/`distinctBy`/`join`/`joinOn`/`leftJoin`/`groupBy`) + terminals (`collect`/`first`/`firstOrElse`/`count`/`fold`/`sum`/`min`/`max`/`average`/`any`/`all`/`forEach`), shared lazily over storage and eagerly in-memory (ADR 0115). **Ordering via a closed orderable-base set** (`Int`/`Float`/`String`/`Duration`/`Instant`), not a typeclass; `sortBy`/`min`/`max` key off the projection. **Empty aggregates return `Option`** (`first`/`min`/`max`/`average`), while `sum`/`count`/`fold` use the identity — fixed at the type because storage learns emptiness only by executing. `average -> Float` (no truncation). **`bynk.list` free functions migrate to methods** + a `bynk-fmt` codemod (deprecate, then remove); `flatMap`'s lambda type flips by provenance. Typeclass `Ordering`, multi-key sort, storage `flatMap` lowering deferred | Accepted (query-algebra track, slice 0 settling) |
| [0117](0117-non-failing-warning-channel.md) | A **non-failing warning channel**: a diagnostic's **severity** (`Severity::for_error(category)`) now decides whether it **fails the build** — a `Warning` surfaces but compile/check **succeed** (exit 0), an `Error` fails as before. Severity was display/LSP-only; this makes it load-bearing. The split is **one partition at the `check`/`compile` boundary** (call sites keep pushing to one sink); `compile`/`compile_project` carry **warnings on success** (`Compiled.warnings` / `ProjectOutput.warnings`). The two existing warning-category errors (`orphan_doc_block`/`unused_capability`) become **true warnings** (fixtures 42/66 move to positive + `expected_warnings.txt`, a new harness surface). `-Werror` deferred. Unblocks the `bynk.list` deprecation (ADR 0116 D6) and `@indexed` hygiene warnings | Accepted (diagnostics) |
| [0118](0118-indexed-indexing-model.md) | The **`@indexed` indexing model** (query-algebra; settles track Q7). `@indexed(by: k, …)` declares **runtime-maintained secondary indexes** — a sibling posting-list `Record<k, PrimaryKey[]>` in the agent state, updated in the **same staged write-set** (ADR 0109) so an indexed map is no less atomic. The **compiler routes** an equality `filter`/`joinOn` on an indexed key to an index lookup, else a scan (range routing deferred). **Index hygiene is build-time WARNINGS** (via ADR 0117, not errors): `bynk.index.missing`/`unused`/`ambiguous`, with add/remove auto-fixes; ambiguity breaks to the **most selective by a static structural heuristic** (no cost-based stats). Honest scope: under wholesale `Record` persistence (ADR 0110 D3) an index is a **CPU** win (O(1) vs O(n)), an **I/O** win only when per-entry DO keys land — same surface either way. Lowering in ADR 0119 | Accepted (query-algebra track, slice 0 settling) |
| [0119](0119-durable-object-query-lowering.md) | **Durable-Object query lowering** (query-algebra; settles Q9/Q10). A lazy `Query[T]` lowers to a **pipeline over the agent's in-memory state `Record`(s)** — reusing the slice-1 combinator TS shapes; the only differences are the **source** (`Object.values(map)`, narrowed by an index posting list, ADR 0118) and the `Effect`-typed terminal reading **staged** state (ADR 0109, read-your-writes). **Scan** = `Object.values` → pipeline; **index lookup** = posting list → map-back; **`flatMap`** = correlated scan (join-rewrite deferred); **joins** = hash joins over the Records, incl. **cross-shape `Map × Log`** (the Log narrows by its implicit **time index** first, then probes the Map's index). Strictly **intra-agent**, over staged state (ADR 0115 D6). Written as source→pipeline so per-entry DO keys (ADR 0110 D3) replace only the source for the I/O win | Accepted (query-algebra track, slice 0 settling) |
| [0120](0120-join-group-combiner-form.md) | **Joins & grouping take a combiner, not a pair type** (query-algebra; slice 4 settling; closes Q13). bynk has **no anonymous product type** and stays nominal, so `joinOn`/`leftJoin`/`join`/`groupBy` each take a trailing **`into:` projector** that names the result: `joinOn(other, left, right, into: (T, U) -> V) -> …[V]`, `leftJoin(…, into: (T, Option[U]) -> V)`, `join(other, on, into: (T, U) -> V)`, `groupBy(key, into: (K, List[T]) -> V) -> …[V]`. The join row exists only **inside** `into` (no tuple to name/destructure); this **supersedes** ADR 0116's `(T, U)` rows + D7 (`groupBy.collect -> List[V]`, not `Map`). Combiners **compose multi-way joins flatly** (no `.0.0` nesting). Lowering (ADR 0119) unchanged but emits `into(t, u)`; an equi-`joinOn` on an `@indexed` key still routes (ADR 0118). **Scope:** `Map`/`Query` same-shape joins + `groupBy` in slice 4; cross-shape `Map × Log` deferred with the `Log` slice. A general n-ary tuple is a named deferral | Accepted (query-algebra track, slice 4 settling) |
| [0121](0121-log-append-and-retention.md) | **`Log[T]` is an append-only, time-indexed sequence** (storage track, slice 4). Persisted as an ordered JSON **array** `[{ t, v }]` (not a `Record` — order is intrinsic), committed wholesale (ADR 0109). `append(e)` stamps `Clock.now()` (`Instant`, ADR 0114), requires **`given Clock`**, and is the **one non-idempotent** storage write (dedup key / §12 `Idempotency` is the referenced-not-enforced safe-use story). Reads are lazy `Query[T]` over the entry **values** with `Log`-specific roots — `since`/`before`/`between(Instant…)`, `recent(n)`, `reversed` — composing with the general vocabulary (ADR 0116); window args are **explicit `Instant`s**, so **reads need no clock** (narrower than `Cache`). **`@retain(d)` is optional** (a `Log` may keep everything) and **prunes on append**, keeping reads clock-free; it flips the second annotation gate functional. Lowering extends ADR 0119 (scan = the array; time index = append order); the cross-shape **`Map × Log` join** (ADR 0119 D6 + 0120 combiner) lands here. Timestamp-exposure, secondary `@indexed`, active retention, per-entry keys deferred | Accepted (storage track, slice 4) |
| [0122](0122-queue-is-a-delivery-concern.md) | **`Queue` is not a storage kind** (storage track, Q5). A queue's defining property is **asynchronous, at-least-once delivery** — a runtime concern, not agent-owned atomically-committed state. §1's architecture (§158/§382) already decomposes "queue" along the services-vs-agents line: **inbound** is the shipped **`from Queue` service protocol** (ADR 0078), **outbound** is a runtime **capability** — neither a `store` field. So `Queue` is **removed from the storage catalogue** (now **five kinds**: `Cell`/`Map`/`Set`/`Log`/`Cache`, all shipped) and joins `Ref[A]`/`Held[T]`/`Connection[F]` in the **held-resources / delivery track** (to be opened), with the §12 `Idempotency` contract. The agent-local "durable work queue" need is `Log` + a cursor `Cell`. Closes the storage *catalogue*; the parity slice (`state{}` removal) and rehydration Q6/Q7 are the track's last open work | Accepted (storage track, Q5) |
| [0123](0123-state-block-cutover-and-codemod.md) | **The parity slice** (storage track) — settles the `state { }` → `store` hard cutover ADR 0108 deferred. **One cutover fully removes** `state { }`, the `commit { }` statement, the `commit` keyword, and `self.state` reads — parser/AST/checker/emitter all drop them; a leftover is a parse/hard error (D1). **Amended:** no shipped codemod — the only Bynk source is this repo's corpus, so it is migrated **by hand, once, in the cutover PR** (`state{f:T}` → `store f: Cell[T]`; `commit{…,f:v}` → `f := v`/`.update` — trivial across ~2 fixtures); this supersedes ADR 0108 D2's `bynk-fmt` codemod, which would parse the legacy surface forever for no caller (D2). Removal + migration land **atomically** (D3). Invariant predicates finalised as **bounded single-element reads** (`Cell` deref, keyed `map.get`/`set.contains`); **whole-collection quantifiers stay deferred** (an O(n)-per-commit scan — a distinct decision the slice doesn't need) (D4) | Accepted (storage track, parity slice) |
| [0124](0124-rehydration-validation-and-migration.md) | **Rehydration validation** (storage track, Q6/Q7 — the track's last open question). Realises the committed-but-unbuilt promise that loading agent state validates its refined fields (today's `loadState` is a raw `stored ?? zero()` cast). **D1:** the gate validates loaded state's shape + refinements against the **current** type definition, automatically, reusing the boundary validator; it runs only when stored state exists (a fresh key's zero is valid). **D2 (Q6):** a failure is an **internal fault — `RehydrationViolation`, the load-time twin of `InvariantViolation`** (500-class, logged with agent + field, never key/value), **not** a caller-facing `BoundaryError`/400 — because the supplier is trusted past-self, not the untrusted caller, and there is no caller contract slot for corrupt self-state. **D3 (Q7):** a refinement that **tightens** across a deploy faults on load (orphaned data is indistinguishable from corruption); breaking migrations are **by convention** (widen-don't-narrow / explicit migration), aligned with the events track — **no coercion, no silent drop, no v1 migration hook**. **D4:** additive evolution is automatic — the load **merges** `{ ...zero(), ...stored }` so a new zeroable `store` field defaults rather than reading `undefined` (fixes a latent load bug). **D5:** settle-only; the rehydration slice builds it; a versioned-schema migration capability, per-field default-on-read, and a soft recovery handler are deferred | Accepted (storage track, rehydration slice) |
| [0125](0125-cell-update-operation.md) | **`Cell[T].update(f)`** — the method-shaped read-modify-write, closing a half-built gap (§2.7.3 defined it and the self-`:=` diagnostic steered to it, but no checker/emitter branch resolved it). **D1:** `update(f: (T) -> T)` is the **one** callable `Cell` op; `read`/`write` stay sugar (bare name / `:=`), not methods. **D2:** it returns **`Effect[()]`**, not `Effect[T]` — it mutates the cell; read-modify-write-**and-return** is the explicit two steps (await `update`, then read the bare name back). **D3:** dispatched by **receiver provenance**, `check_store_cell_op` a sibling of the `Map`/`Set` helpers (`bynk.store.unknown_op` for anything but `update`). **D4:** lowered to a **staged read-modify-write** over `__state.<cell>` — `Map.update`'s lowering minus the key-absent guard — committed by the **same end-of-handler flush** through the invariant gate that `:=` uses (ADR 0109); no new runtime helper. **D5:** the combiner is pure for free (a non-effectful `Ty::Fn`; an effectful body, including a bare read of another cell, is rejected at the existing gate). **D6:** the `bynk.cell.self_reference` diagnostic stays — its suggested fix now compiles. **D7:** settles the `Cell` operations in §2.7.3 from *Open* to **normative** | Accepted (post-storage-track language increment) |
| [0126](0126-httpresult-rfc9110-status-vocabulary.md) | **`HttpResult` tracks the RFC 9110 status vocabulary**, and redirects gain a **`Location` payload shape**. Two shipped examples hit the old 10-variant ceiling (`rate-limiter` wanted `429`, `link-shortener` wanted a `302`). **D1:** `HTTP_VARIANTS` widens 10 → **29** — `Accepted` (202); the redirects 301/302/303/307/308; the 4xx additions 405/406/408/410/411/413/415/`429`/451; and 5xx 501/502/503/504 — a **curated, not full-IANA** set (1xx, conditional 304, never-handler-authored codes stay out). **D2:** a redirect's outcome is a `Location` header over an **empty body**, so `HttpVariantPayload` gains **`Location`** (a `String` URL, validated like `Message`, emitted as `Response(null, { headers: { location } })`, bound as `location`) — the first non-body, header-carrying shape. **D3:** the registry stays the **single source of truth** — resolver/LSP-completion (ADR 0093)/emitter pick up a new row for free; the only exhaustive matches are the payload-shape arms (construction, pattern-binding, runtime status map), each extended once. **D4:** `Message` (caller-helpful faults + 429) vs `None` (self-describing statuses) is a per-status editorial call; an out-of-set code has **no constructor** — a deliberate, re-openable boundary. **D5:** **additive** — no variant renamed/removed (`ServerError` keeps its name), so no new diagnostics and **no version bump**; a refined `Url` widens to the redirect's `String` argument by the existing rule | Accepted (HTTP status-vocabulary increment) |
| [0127](0127-capability-requirement-provenance.md) | **Capability requirements gain generic provenance, surfaced as a materializable ghost `given`; and `by` is rejected on an agent handler.** A store `Cache`/`Log` op's `given Clock` requirement is real but **invisible at the declaration site**. **D1:** model a requirement as `{ capability, site, source }` and render the reason as a **total function of `source`** (`DirectCall{op}` → *"calls `Cap.op`"*, correct for **any** capability incl. user-defined; `StoreOp{kind,op}` → a storage-feature fragment table; `Builtin{feature}`) — **adding a capability needs zero new reason text**; only a new capability-*consuming feature* authors a fragment (totality-tested). **D2:** generalise `require_clock` → `require_capability`, **recording every** requirement (covered or not) into a per-file ledger (the capability analogue of the inferred-type `HintSink`, ADR 0056) and still erroring when undeclared; threads through the checker and drains into `ProjectDiagnostics`. **D3:** the affordance is a **materializable inlay hint** — a ghost `«given Clock»` at the handler signature whose `text_edits` write the real clause via `given_insertion_edit` (ADR 0054); a source-level `@requires` is **rejected** (it restates a derivable internal). **D4:** reject `by` on an agent `on call` handler (`bynk.actor.by_on_agent`) — `by` is a service-edge clause, an agent has no actor; turns the boundary taxonomy's "actor auth never crosses" into a checked invariant (**zero** blast radius). The sibling **capability-encapsulation boundary** (the `tsc`-soundness fix) is promoted to the `agent-capability-encapsulation` track — its spike showed it pulls in a new bundle composition root | Accepted (capability-provenance increment) |
| [0128](0128-stream-value-over-time-primitive.md) | **`Stream[T]`, the value-over-time primitive** — the first slice of the real-time/WebSocket track. The language had `Effect[T]` (a once-resolving value) and `Query[T]` (a snapshot read) but no word for a value produced **incrementally over time**. **D1:** a new built-in parametric **value** type, modelled on `Query[T]` (`TypeRef::Stream`/`Ty::Stream`), **not** an `Effect` variant (it resolves many times) and **not** a `Query` (no snapshot); covariant in its element, so nameable/returnable/passable. **D2:** classified **non-serialisable/non-storable/non-boundary/non-comparable** by one arm per existing site (`json_codable`, store-field admission, boundary validation), with two new diagnostics `bynk.types.stream_at_boundary` and `bynk.types.stream_not_comparable` (the latter guards the `==` arm, which reuses `compatible` and would otherwise accept an assignable `Stream`). **D3:** minimal vocabulary `Stream.of`/`map`/`take`/`collect` — `collect` (drain → `Effect[List[T]]`) goes one step beyond the track's stated `of`/`map`/`take` because a builder-only slice is untestable (stated openly); a fuller algebra earns its own slice. **D4:** errors ride **in-band** as `Result` elements (`Stream[Result[T, E]]`), no side channel. **D5:** keep the **type parameter** now, **defer the element semantics** (cross-boundary transport/serialisability) to the first non-chunk consumer — slice 0 never crosses a boundary with an element. **D6:** lowers to `AsyncIterable<T>`, emitted **inline** (async-generator IIFEs, no runtime import), so non-stream files are byte-identical and strip-only holds; `collect` → `Promise<T[]>`. **D7:** the constructor is the static **`Stream.of(xs)`** (mirroring `Duration.millis`/`Instant.fromEpochMillis`), not a `bynk.stream.*` free function (ADR 0116 D6 deprecated that style) | Accepted (real-time track, slice 0) |
| [0129](0129-streaming-http-response.md) | **streaming HTTP response** — the real-time track's slice 1 and `Stream[T]`'s first end-to-end consumer, on `from http` with no socket/DO. **D1:** a fifth `HttpResult` payload shape **`Streamed`** + one **`Streaming` (200)** variant carrying a `Stream[String]`, mirroring ADR 0126 D2's `Location` precedent (a shape realised by dedicated status rows) — **not** a polymorphic `Ok` (breaks 0126's one-variant-one-shape model) nor a status-less peer; extends the three exhaustive arms (construction → `HttpResult[()]`, pattern-bind `stream`, runtime status map) once each. **D2 (sharpens the track's Q2):** streamed responses are **200-only** — a response commits its status before the first chunk, so pre-stream failures are ordinary variants returned *instead* (they share `HttpResult[()]`, coexisting in one handler) and mid-stream failures are in-band `Result` elements the producer `.map`s to `Stream[String]`. **D3:** the body is a `Stream[String]`, each element one SSE `data:` event (`text/event-stream`, `cache-control: no-cache`); a structured `SseEvent` is a follow-on. **D4:** SSE framing is a runtime `sseResponse` helper (source `runtime/src/http.ts`, regenerated into the shipped `emitter/runtime.ts` by the bundler/drift guard) wrapping the `AsyncIterable<string>` in a `ReadableStream<Uint8Array>` — a Web standard on Workers + Node; emitter dispatch unchanged | Accepted (real-time track, slice 1) |
| [0130](0130-held-resource-linearity.md) | **held-resource linearity** — the real-time track's slice 2, realising `bynk-type-system.md` §2.9 (settled-in-shape since the design notes) and its §3 step 11. **D1:** `Connection[F]`, the one instance of a closed `Held` kind (`Ty::is_held`); joins the non-serialisable family — non-serialisable/non-boundary/non-comparable (`held_not_comparable`). **D2:** ops `c.send(f)` (non-consuming, frame-typed) and `c.close()` (consuming, a method — refines §2.9's `close(conn)`). **D3:** storage admission — held admitted in `Cell[Option[Connection]]`/`Map[K, Connection]` (exception to serialisability; hibernation preserves them, §2.9.6), rejected in `Set`/`Log`/`Cache` (`held_unsupported_storage`); `put` consumes, `remove` removes-and-closes. **D4:** the linearity check is a **flow-sensitive post-pass** (§3 step 11, after `type_of_block` over `expr_types`) threading owned→borrowed→consumed: single-owner, mandatory disposal (`held_leak`), no use-after-consume (`held_use_after_consume`), branch unification (`held_branch_divergence`, the Q6 diag) — bounded (fixed vocabulary, 3 states, no general lattice), separate from the type walk whose branches pop scopes before unifying. **D5 (Q5):** within-handler fault paths settled — owned-at-abort implicitly runtime-consumed, stored rolls back (ADR 0109), compensation-on-still-local rejected; cross-context deferred to slice 3. **D6:** emit against a runtime `Connection<F>` interface; the `TestConnection`/hibernatable-WS impls land with the protocol (slice 3). Compile-time slice — tested against a hand-written capability source, no socket | Accepted (real-time track, slice 2) |
| [0131](0131-from-websocket-protocol-bundle.md) | **the `from WebSocket` protocol (bundle vertical)** — the real-time track's slice 3a (the security-bearing one; `/security-review`-gated), split from the full slice 3 so the surface + checker + a runnable bundle vertical land first and the Workers hibernatable upgrade (3b) follows. **D1:** `ServiceProtocol::WebSocket { in_type, out_type }` + a `HandlerKind::Open`; exactly one `on open` per service (`websocket_open_arity`), inbound frames are the agent's typed messages; `on open` receives a synthetic owned `connection: Connection[out]` governed by the slice-2 linearity pass (an undisposed one is `held_leak`). **D2 (crux):** edge auth **before accept**, `by` **mandatory** — like HTTP, no safe default actor; an anonymous `on open` is a compile error; accept-then-authenticate is unrepresentable. **D3 (Q8 static half):** the WS boundary admits `None`/`Bearer`, **rejects `Signature`** (`scheme_not_admissible`) — a browser can't set `Authorization`, so Bearer reads from `Sec-WebSocket-Protocol`; HMAC-over-body has no handshake body. **D4:** the bundle vertical — `on open` lowers to a callable `open(connection, …)` method; the runtime ships **`TestConnection`** (capture-and-inspect `Connection<F>`); the §20 chat-room **runs** under node (welcome frame captured, connection transferred to the agent). **D5:** `--target workers` is platform-locked off (`websocket_workers_unsupported`) until 3b (the Durable Object hibernatable upgrade — `acceptWebSocket`, `serializeAttachment` re-association Q7, frame dispatch). The proposal is retained until 3b | Accepted (real-time track, slice 3a) |
| [0132](0132-from-websocket-protocol-workers.md) | **the `from WebSocket` Workers wire path** — the real-time track's slice 3b-i (security-bearing; `/security-review` + `/code-review`), the Workers production half of slice 3, split from the hibernation re-association (3b-ii). **D1 (crux):** a live socket can't cross a DO RPC and hibernation needs it in the DO, so the **upgrade request moves, not the socket** — the Worker **authenticates the actor at the edge** and forwards to the hosting DO only on success (verified identity in a trusted, non-forgeable internal header; the DO is reachable only through the Worker). **D2:** the hosting DO is resolved statically from the **single connection transfer** (`Room(roomId).join(…, connection)` → `ROOM` keyed by `roomId`; zero/multiple/non-routable → `ws.open_transfer_shape`); inside the DO the transfer lowers to a **`this`-self-call**. **D3 (Q8 runtime):** the Bearer token reads from the first **`Sec-WebSocket-Protocol`** element, verified fail-closed (`401`) with the **same audited verifier** HTTP uses, authorization predicate `403`, route params validated via `.of` (`400`) — all before forwarding; the scheme may be `None` (anonymous, like an HTTP `Visitor` route). **D4:** a held `store Map[K, Connection]` can't be JSON-persisted, so on Workers it lives in an **in-memory side-table** (`heldStore`, keyed by the durable state object) split out of the persisted record — lost on eviction, the **non-hibernatable** model. **D5:** the upgrade routes by an `Upgrade: websocket` header; route params come from the query string (v1, one WS service per context). **D6:** runtime `WorkersConnection<F>` + `WebSocketPair` helpers, no `@cloudflare/workers-types`. **Deferred to 3b-ii:** hibernation re-association (`acceptWebSocket`/`serializeAttachment` Q7), inbound `webSocketMessage` dispatch, broadcast. Removes the 3a platform-lock | Accepted (real-time track, slice 3b-i) |
| [0133](0133-from-websocket-hibernation.md) | **`from WebSocket` hibernation re-association** — the real-time track's slice 3b-ii (security-bearing; `/security-review` + `/code-review`), the **Q7** piece ADR 0132 deferred. A held `Connection` survives Durable Object eviction (§2.9.6). **D1 (crux):** the stored value is the **connId, not the socket** — accept via `state.acceptWebSocket(server, [connId])` (fresh `crypto.randomUUID()`, hibernatable) not `server.accept()`; every access re-resolves connId → live socket via `getWebSockets`, so no live socket is held across requests. **D2:** a held `Map[K, Connection]` persists `Record<string(K), connId>` (reverses 3b-i's in-memory split — the value is serialisable now), rejoining the state record / zero / rehydration key-check / commit; `put` stores `connIdOf(conn)`, `get` resolves (or `None`), iteration resolves-present. **D3:** resolution is fail-soft — a connId with no live socket is `None`, not a fault (a closed connection is normal lifecycle). **D4:** `remove` resolves-closes-deletes (finally emits the §2.9 "removes-and-closes" contract — closes the 3b-i follow-up). **D5:** bundle unchanged (in-memory `TestConnection`); connId representation Workers-only. Runtime `acceptHibernatableConnection`/`resolveConnection`/`connIdOf` replace the 3b-i `heldStore`. **Deferred to 3b-iii:** inbound `webSocketMessage` dispatch (a new protocol surface) | Accepted (real-time track, slice 3b-ii) |
| [0134](0134-from-websocket-inbound.md) | **`from WebSocket` inbound `on message`/`on close`** — the real-time track's slice 3b-iii (security-bearing; `/security-review` + `/code-review`), the receive/close half ADR 0133 deferred. **D1:** `on message`/`on close` are **service** handlers (like `on open`), DO-hosted (self-agent lowering); the `by` identity + route args come from the socket `serializeAttachment` (extended to `{ connId, identity, args }`, written at `on open`) — **not** re-derived from the frame, **not** re-verified per message (authenticated once). **D2:** the inbound frame is decoded against `in:` fail-closed in `webSocketMessage` — a malformed frame closes the socket (`1003`/`1008`) and is never dispatched (the client-bytes trust boundary). **D3:** the firing `connection` is a **borrowed** held binding — `send` allowed, `close`/transfer rejected (`held.consume_on_borrow`), no disposal obligation (a new `borrowed_held` set in the linearity pass). **D4:** the `on message` frame is the param typed as the service `in` (`ws.message_frame_param` otherwise); rest are route values from the attachment; ≤1 message/close each — reuses the single-param-list parser (no syntax change). **D5:** `on close` is an optional domain hook (a closed socket resolves to `None` fail-soft; no auto-prune). **D6:** bundle — callable handlers a `TestConnection` test drives (the inbound echo runs green). New `HandlerKind::Close`; a WS `on message` reuses `HandlerKind::Message` (disambiguated by protocol). **Deferred to slice 4:** broadcast-to-all-connections + the full §20 chat-room end-to-end | Accepted (real-time track, slice 3b-iii) |
| [0135](0135-ws-broadcast-closure.md) | **real-time track closure — `parTraverse` + the §20 chat-room end-to-end** (slice 4; **retires the track**). The held-aware iteration broadcast (`conns.forEach((c) => c.send(frame))`) already compiled on both targets (slice-2 closure borrow + 3b-ii connId resolution); slice 4 closes it. **D1:** **`parTraverse`** — the parallel broadcast primitive (`Promise.all(xs.map(f))`), type-identical to the sequential `forEach` but concurrent (a slow client must not head-of-line-block the room); the §20 form. **D2:** exclude-self **by key** (`UserId`), not `c != conn` — `Connection` stays non-comparable (`held_not_comparable`). **D3:** fixes a latent gap — the store-map `forEach`/`parTraverse` receiver's `Query[V]` type was never recorded, so the closure-param **borrow was silently unenforced** (`forEach((c) => c.close())` compiled); now recorded, so `send` is allowed and `close`/transfer is `held.consume_on_borrow`. **D4:** bare-map iteration is the v1 surface; the `.values` accessor (resolves as a cross-context path today) + lambda param-type inference are named ergonomic follow-ons. **Proof:** the §20 chat-room runs end-to-end on bundle — two participants join a room, a message fans out to both, one leaves and the next reaches only the other (behaviour test, green under node); Workers stays at `tsc --strict` + the strip-types guard. **The track that began with `Stream[T]` is complete.** | Accepted (real-time track, slice 4) |
| [0136](0136-strip-only-emission-invariant.md) | **The emitter's output is strip-only** — the first slice of the in-browser track, front-loading the work for an in-browser REPL/playground. Pure type-stripping (Node `--experimental-strip-types` / `node:module` `stripTypeScriptTypes`) erases type syntax but **not** type-directed constructs — constructor **parameter properties**, `enum`, `namespace` — which `tsc` accepts, so the `tsc --strict` gate misses them. **D1:** the emitter emits only erasable TS across its whole surface, a standing invariant, not a per-target branch. The provider `given` constructor de-sugars **unconditionally** from `constructor(private deps: {…})` to a declared typed field + assigning constructor (under ES2022 `useDefineForClassFields`, end state identical, strips clean) — *removing* the one type-directed site rather than branching. **D2:** the same de-sugaring fixes the three shipped bindings (`bynk-{cloudflare,node}.ts`, `cloudflare.binding.ts`), whose `constructor(private env?: unknown)` had silently broken every `--inspect` debug session that touched `Secrets`/a `given` provider (audit gap — the track had named only the emitter site). **D3:** a regression test makes the invariant load-bearing — every emitted `.ts` across the project fixtures is checked with `stripTypeScriptTypes(code, { mode: 'strip' })` (one process over the staged tree); `node --check` is *not* used (a leading `type`/`declare` false-fails it). Complements ADR 0104 (the `--inspect` build runs emitted `.ts` strip-only); leaves TS-first output untouched | Accepted (in-browser track, slice 0; v0.108) |
| [0137](0137-first-class-js-artefact.md) | **A first-class JavaScript artefact** — the in-browser track's slice 1: `bynkc compile --emit js` (default stays `ts`). Because the emitter is strip-only (ADR 0136), a JS artefact is *emit-then-strip* — the same output with type syntax erased. The external-`tsc` route was rejected (it reintroduces the Node/`tsc` dependency the track exists to remove and cannot run in-browser). **D1:** the stripper is **oxc** (pure-Rust TS parse + type-erase + codegen, wasm-safe so the wasm slice reuses it in-browser) in a **dedicated `bynk-strip` crate**, keeping oxc out of `bynk-emit` and thus the LSP; a hand-rolled stripper was rejected (the `:`/`<…>`/`as`/`type`-specifier ambiguities need a real parser). **D2:** **pure type-stripping** (`only_remove_type_imports`) — every *value* import is preserved even if unused, only `import type`/`type` specifiers and type syntax are erased, matching Node's `stripTypeScriptTypes` (the slice-0 oracle) rather than TypeScript's usage-based elision. **D3:** stripping is a **post-emit step in `bynkc`** (`strip_project_to_js`), not an emitter mode — `bynk-emit` stays TS-only; `.ts`→`.js`, `tsconfig.json` dropped, maps/debug sidecar dropped, other files (`wrangler.toml`) passed through; import specifiers are already `.js`. Verified by `node --check` over every emitted `.js` (also a residue check). TS-first output untouched; JS is additive | Accepted (in-browser track, slice 1; v0.108.1) |
