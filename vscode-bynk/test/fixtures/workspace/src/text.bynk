commons fixture.text

-- `shout` is referenced by `greet`, so go-to-references and the
-- "N references" CodeLens have something to resolve.
fn shout(s: String) -> String {
  s
}

fn greet(name: String) -> String {
  "Hi, \(shout(name))!"
}
