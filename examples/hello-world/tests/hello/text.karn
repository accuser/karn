test hello.text {
  test "greets the world" {
    assert greeting("World") == "Hello, World!"
  }

  test "greets any valid subject" {
    assert greeting(Mock[Subject]("Karn")) == "Hello, Karn!"
  }

  test "rejects an empty subject" {
    assert Subject.of("") is Err(_)
  }
}
