suite commerce.money {
  case "block-arm expect" {
    let m = Money.fromMinorUnits(10)
    match m {
      Ok(_) => {
        expect true
      }
      Err(_) => {
        expect false
      }
    }
  }
}
