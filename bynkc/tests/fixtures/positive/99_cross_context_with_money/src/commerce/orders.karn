context commerce.orders

uses commerce.money
consumes commerce.payment

service placeOrder {
  on call(total: Money) -> Effect[Result[Int, Int]] {
    let _ <- commerce.payment.authorise(total)
    Ok(0)
  }
}
