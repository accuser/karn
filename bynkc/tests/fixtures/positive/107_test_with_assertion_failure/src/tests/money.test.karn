test commerce.money {
  -- This test is intentionally written to fail at runtime so the
  -- runner's failure reporting is exercised.
  test "deliberate failure" {
    let m = Money.fromMinorUnits(10)
    assert m is Err(_)
  }
}
