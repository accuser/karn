context ticker

service clock from cron {
  -- v0.10a: a cron handler may take the scheduled fire time (epoch ms) as an
  -- optional `Int` parameter.
  on schedule("*/5 * * * *") (at: Int) -> Effect[Result[(), String]] {
    Ok(())
  }
}
