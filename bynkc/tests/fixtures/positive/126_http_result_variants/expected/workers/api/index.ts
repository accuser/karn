// Generated by bynkc — do not edit by hand.
// Worker entry point for context `api`.

import { Ok, Err, type Result, type JsonValue, type BoundaryError, boundaryError, matchPath, httpResultToResponse } from "../../runtime.js";
import { compose, type Env } from "./compose.js";
import * as handlers from "./handlers.js";

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    const url = new URL(request.url);
    const path = url.pathname;
    const method = request.method;
    const surface = compose(env);
    try {
      if (path.startsWith("/_bynk/call/")) {
        const servicePath = path.slice("/_bynk/call/".length);
        switch (servicePath) {
          default:
            return new Response("Not found", { status: 404 });
        }
      }

      {
        if (method === "GET" && path === "/badrequest") {
          const result = await surface.http_GET_badrequest();
          return httpResultToResponse(result, (v: any) => v as JsonValue);
        }
      }
      {
        if (method === "GET" && path === "/conflict") {
          const result = await surface.http_GET_conflict();
          return httpResultToResponse(result, (v: any) => v as JsonValue);
        }
      }
      {
        if (method === "GET" && path === "/created") {
          const result = await surface.http_GET_created();
          return httpResultToResponse(result, (v: any) => v as JsonValue);
        }
      }
      {
        if (method === "GET" && path === "/forbidden") {
          const result = await surface.http_GET_forbidden();
          return httpResultToResponse(result, (v: any) => v as JsonValue);
        }
      }
      {
        if (method === "GET" && path === "/nocontent") {
          const result = await surface.http_GET_nocontent();
          return httpResultToResponse(result, (v: any) => v as JsonValue);
        }
      }
      {
        if (method === "GET" && path === "/notfound") {
          const result = await surface.http_GET_notfound();
          return httpResultToResponse(result, (v: any) => v as JsonValue);
        }
      }
      {
        if (method === "GET" && path === "/servererror") {
          const result = await surface.http_GET_servererror();
          return httpResultToResponse(result, (v: any) => v as JsonValue);
        }
      }
      {
        if (method === "GET" && path === "/unauthorized") {
          const result = await surface.http_GET_unauthorized();
          return httpResultToResponse(result, (v: any) => v as JsonValue);
        }
      }
      {
        if (method === "GET" && path === "/unprocessable") {
          const result = await surface.http_GET_unprocessable();
          return httpResultToResponse(result, (v: any) => v as JsonValue);
        }
      }
      return new Response("Not Found", { status: 404 });
    } catch {
      return new Response("Internal Server Error", { status: 500 });
    }
  },
};
