// Generated by karnc — do not edit by hand.
// context ids.gen

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "../runtime.js";

import { Uuid } from "../karn.js";

import * as karn from "../karn.js";

export const generate = {
  async call(deps: { Random: karn.Random }): Promise<Uuid> {
    const id = await deps.Random.uuid();
    return id;
  },
};

export interface IdsGenDeps {
  readonly Random: karn.Random;
}

export function makeSurface(deps: IdsGenDeps) {
  return {
    async generate(): Promise<Uuid> {
      return generate.call(deps);
    },
  };
}

