context ids.gen

consumes bynk { Clock, Secrets }

service stamp {
  on call() -> Effect[Int] given Clock, Secrets {
    let salt <- Secrets.get("STAMP_SALT")
    let now <- Clock.now()
    match salt {
      Some(_) => now + 1,
      None => now,
    }
  }
}
