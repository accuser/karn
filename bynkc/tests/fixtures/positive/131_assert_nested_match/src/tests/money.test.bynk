suite commerce.money {
  case "nested match expect" {
    let outer = Money.fromMinorUnits(10)
    let inner = Money.fromMinorUnits(-1)
    match outer {
      Ok(_) => match inner {
        Ok(_) => expect false
        Err(_) => expect true
      }
      Err(_) => expect false
    }
  }
}
