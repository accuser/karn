// Generated by karnc — do not edit by hand.
// adapter weather

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "./runtime.js";

import * as karn from "./karn.js";

export interface Report {
  readonly tempC: number;
  readonly summary: string;
}

export const Report = {
};

export type WeatherError =
    { readonly tag: "NotFound" }
  | { readonly tag: "Upstream" };

export const WeatherError = {
  NotFound: { tag: "NotFound" } as WeatherError,
  Upstream: { tag: "Upstream" } as WeatherError,
};

export interface Weather {
  current(city: string): Promise<Result<Report, WeatherError>>;
}

export const WeatherToken: unique symbol = Symbol("Weather");

