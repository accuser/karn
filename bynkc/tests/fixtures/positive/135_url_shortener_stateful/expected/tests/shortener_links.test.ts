// Generated by bynkc — do not edit by hand.
// test target: shortener.links

import { Ok, Err, Some, None, makeTestState, type Result, type Option, type ValidationError } from "../runtime.js";
import * as shortener_links from "./../shortener/links.js";
import * as shortener_analytics from "./../shortener/analytics.js";
import * as shortener_core from "./../shortener/core.js";

class ExpectationError extends Error {
  location: string;
  start: number;
  end: number;
  constructor(location: string, start: number, end: number, detail: string) {
    super(`${detail}\n  at ${location}`);
    this.location = location;
    this.start = start;
    this.end = end;
  }
}
function __bynkExpectFailure(location: string, start: number, end: number, detail: string) {
  return new ExpectationError(location, start, end, detail);
}
function __bynkExpect(cond: boolean, location: string, start: number, end: number, detail: string): void {
  if (!cond) { throw __bynkExpectFailure(location, start, end, detail); }
}
function __bynkShow(v: unknown): string {
  try { return typeof v === "bigint" ? String(v) : (JSON.stringify(v) ?? String(v)); } catch { return String(v); }
}

class MockAnalytics {
  async track(code: shortener_analytics.ShortCode): Promise<Result<number, shortener_analytics.AnalyticsError>> {
    const { AnalyticsError } = shortener_analytics as any;
    return Ok(1);
  }
}

class TestCodeGen {
  async next(): Promise<string> {
    const { CreateLinkRequest, CreatedView, LinkError, ResolveView } = shortener_links as any;
    return "test01";
  }
}

function makeTestDeps() {
  return { CodeGen: new TestCodeGen(), surface: { Analytics: new MockAnalytics() } };
}

async function test_a_fresh_Link_key_resolves_to_NotFound() {
  try {
    shortener_links.__resetAgents();
    const deps = makeTestDeps();
    const { CreateLinkRequest, CreatedView, Link, LinkError, ResolveView, __makeLink, api, create } = shortener_links as any;
    const { ShortCode, Url } = shortener_core as any;
    const { AnalyticsError } = shortener_analytics as any;
    const Analytics = (deps as any).surface?.Analytics;
    void (await (async (__d) => {
        switch (__d.tag) {
          case "Err": {
            return __bynkExpect((false), "tests/shortener/links.bynk:50:20", 1665, 1670, "expect false");
          }
          case "Ok": {
            const code = __d.value;
            const link = __makeLink(code);
            const outcome = await link.resolve(deps);
            switch (outcome.tag) {
              case "Ok": {
                return __bynkExpect((false), "tests/shortener/links.bynk:55:21", 1784, 1789, "expect false");
              }
              case "Err": {
                const linkError = outcome.error;
                return ((__d) => {
        switch (__d.tag) {
          case "NotFound": {
            return __bynkExpect((true), "tests/shortener/links.bynk:57:30", 1859, 1863, "expect true");
          }
          case "AlreadyExists": {
            return __bynkExpect((false), "tests/shortener/links.bynk:58:30", 1893, 1898, "expect false");
          }
        }
        throw new Error("non-exhaustive match");
      })(linkError);
              }
            }
            throw new Error("non-exhaustive match");
          }
        }
        throw new Error("non-exhaustive match");
      })(ShortCode.of("fresh2")));
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

async function test_creating_a_link_stores_the_target_and_returns_its_code() {
  try {
    shortener_links.__resetAgents();
    const deps = makeTestDeps();
    const { CreateLinkRequest, CreatedView, Link, LinkError, ResolveView, __makeLink, api, create } = shortener_links as any;
    const { ShortCode, Url } = shortener_core as any;
    const { AnalyticsError } = shortener_analytics as any;
    const Analytics = (deps as any).surface?.Analytics;
    void (await (async (__d) => {
        switch (__d.tag) {
          case "Err": {
            return __bynkExpect((false), "tests/shortener/links.bynk:67:20", 2035, 2040, "expect false");
          }
          case "Ok": {
            const code = __d.value;
            return await (async (__d) => {
        switch (__d.tag) {
          case "Err": {
            return __bynkExpect((false), "tests/shortener/links.bynk:69:21", 2113, 2118, "expect false");
          }
          case "Ok": {
            const url = __d.value;
            const outcome = await create.call(code, url, deps);
            switch (outcome.tag) {
              case "Ok": {
                return __bynkExpect((true), "tests/shortener/links.bynk:73:23", 2219, 2223, "expect true");
              }
              case "Err": {
                return __bynkExpect((false), "tests/shortener/links.bynk:74:23", 2246, 2251, "expect false");
              }
            }
            throw new Error("non-exhaustive match");
          }
        }
        throw new Error("non-exhaustive match");
      })(Url.of("https://example.com"));
          }
        }
        throw new Error("non-exhaustive match");
      })(ShortCode.of("test01")));
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

async function test_creating_the_same_code_twice_is_rejected() {
  try {
    shortener_links.__resetAgents();
    const deps = makeTestDeps();
    const { CreateLinkRequest, CreatedView, Link, LinkError, ResolveView, __makeLink, api, create } = shortener_links as any;
    const { ShortCode, Url } = shortener_core as any;
    const { AnalyticsError } = shortener_analytics as any;
    const Analytics = (deps as any).surface?.Analytics;
    void (await (async (__d) => {
        switch (__d.tag) {
          case "Err": {
            return __bynkExpect((false), "tests/shortener/links.bynk:83:20", 2374, 2379, "expect false");
          }
          case "Ok": {
            const code = __d.value;
            return await (async (__d) => {
        switch (__d.tag) {
          case "Err": {
            return __bynkExpect((false), "tests/shortener/links.bynk:85:21", 2452, 2457, "expect false");
          }
          case "Ok": {
            const url = __d.value;
            const __r0 = await create.call(code, url, deps);
            const second = await create.call(code, url, deps);
            switch (second.tag) {
              case "Ok": {
                return __bynkExpect((false), "tests/shortener/links.bynk:90:22", 2591, 2596, "expect false");
              }
              case "Err": {
                const linkError = second.error;
                return ((__d) => {
        switch (__d.tag) {
          case "AlreadyExists": {
            return __bynkExpect((true), "tests/shortener/links.bynk:92:31", 2668, 2672, "expect true");
          }
          case "NotFound": {
            return __bynkExpect((false), "tests/shortener/links.bynk:93:31", 2703, 2708, "expect false");
          }
        }
        throw new Error("non-exhaustive match");
      })(linkError);
              }
            }
            throw new Error("non-exhaustive match");
          }
        }
        throw new Error("non-exhaustive match");
      })(Url.of("https://example.com"));
          }
        }
        throw new Error("non-exhaustive match");
      })(ShortCode.of("dup001")));
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

async function test_resolving_a_registered_code_returns_its_target() {
  try {
    shortener_links.__resetAgents();
    const deps = makeTestDeps();
    const { CreateLinkRequest, CreatedView, Link, LinkError, ResolveView, __makeLink, api, create } = shortener_links as any;
    const { ShortCode, Url } = shortener_core as any;
    const { AnalyticsError } = shortener_analytics as any;
    const Analytics = (deps as any).surface?.Analytics;
    void (await (async (__d) => {
        switch (__d.tag) {
          case "Err": {
            return __bynkExpect((false), "tests/shortener/links.bynk:103:20", 2844, 2849, "expect false");
          }
          case "Ok": {
            const code = __d.value;
            return await (async (__d) => {
        switch (__d.tag) {
          case "Err": {
            return __bynkExpect((false), "tests/shortener/links.bynk:105:21", 2927, 2932, "expect false");
          }
          case "Ok": {
            const url = __d.value;
            const __r0 = await create.call(code, url, deps);
            const link = __makeLink(code);
            const outcome = await link.resolve(deps);
            switch (outcome.tag) {
              case "Ok": {
                const target = outcome.value;
                return __bynkExpect((target === url), "tests/shortener/links.bynk:111:27", 3091, 3104, "expect target == url");
              }
              case "Err": {
                return __bynkExpect((false), "tests/shortener/links.bynk:112:27", 3131, 3136, "expect false");
              }
            }
            throw new Error("non-exhaustive match");
          }
        }
        throw new Error("non-exhaustive match");
      })(Url.of("https://example.com/page"));
          }
        }
        throw new Error("non-exhaustive match");
      })(ShortCode.of("res001")));
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

export async function run() {
  const results = [];
  results.push({ name: "a fresh Link key resolves to NotFound", ...(await test_a_fresh_Link_key_resolves_to_NotFound()) });
  results.push({ name: "creating a link stores the target and returns its code", ...(await test_creating_a_link_stores_the_target_and_returns_its_code()) });
  results.push({ name: "creating the same code twice is rejected", ...(await test_creating_the_same_code_twice_is_rejected()) });
  results.push({ name: "resolving a registered code returns its target", ...(await test_resolving_a_registered_code_returns_its_target()) });
  return results;
}
