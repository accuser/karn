// Generated by bynkc — do not edit by hand.
// commons demo.shapes

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "../runtime.js";

export type OrderId = string & { readonly __brand: "OrderId" };

export const OrderId = {
  of(value: string): Result<OrderId, ValidationError> {
    return Ok(value as OrderId);
  },
  unsafe(value: string): OrderId {
    return value as OrderId;
  },
};

export type Status =
    { readonly tag: "Active"; readonly since: number }
  | { readonly tag: "Closed" };

export const Status = {
  Active: (since: number): Status => ({ tag: "Active", since }),
  Closed: { tag: "Closed" } as Status,
};

export interface Cart {
  readonly id: OrderId;
  readonly count: number;
}

export const Cart = {
};

