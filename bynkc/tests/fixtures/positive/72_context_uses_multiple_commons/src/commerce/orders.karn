context commerce.orders {
  uses commerce.money
  uses commerce.identifiers

  exports transparent { Order }

  type Order = {
    id:       OrderId,
    customer: CustomerId,
    total:    Money,
  }
}
