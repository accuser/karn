// Generated by bynkc — do not edit by hand.
// composition root for `payment` Worker.

import { type ServiceBinding } from "../../runtime.js";
import * as handlers from "./handlers.js";

export interface Env {
}

export function compose(env: Env) {
  const Http = new handlers.FetchHttp();
  const Logger = new handlers.ConsoleLogger();
  const Payments = new handlers.StripePayments({ Http, Logger });
  const deps = { Http, Logger, Payments };
  return {
    async authorise(amount: any) {
      return handlers.authorise.call(amount, deps);
    },
  };
}
