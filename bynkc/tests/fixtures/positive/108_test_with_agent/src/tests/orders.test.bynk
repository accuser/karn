suite commerce.orders {
  case "agent place reports Ok" {
    let r <- Order(OrderId.unsafe("ORD-1")).place(50)
    expect r is Ok(_)
  }

  case "agent place reports NotPlaced for zero" {
    let r <- Order(OrderId.unsafe("ORD-2")).place(0)
    expect r is Err(_)
  }
}
