suite commerce.money {
  case "money rejects negative" {
    let m = Money.fromMinorUnits(-1)
    expect m is Err(_)
  }

  case "money accepts positive" {
    let m = Money.fromMinorUnits(10)
    expect m is Ok(_)
  }
}
