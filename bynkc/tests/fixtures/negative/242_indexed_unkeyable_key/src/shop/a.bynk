context shop.a

type R = {
  id: String,
  active: Bool,
}

agent A {
  key k: String
  store m: Map[String, R] @indexed(by: active)

  on call howMany() -> Effect[Int] {
    let n <- m.size()
    Effect.pure(n)
  }
}
