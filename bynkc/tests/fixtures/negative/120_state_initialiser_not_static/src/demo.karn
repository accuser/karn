context demo

type Light = enum { Red, Green }

agent Signal {
  key id: String

  state {
    colour: Light = self.state.colour,
  }

  on call peek() -> Effect[Result[(), String]] {
    Ok(())
  }
}
