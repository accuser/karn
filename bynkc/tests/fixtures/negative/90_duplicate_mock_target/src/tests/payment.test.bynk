suite commerce.payment {
  mocks Logger = First {
    fn log(msg: String) -> Effect[()] {
      Effect.pure(())
    }
  }

  mocks Logger = Second {
    fn log(msg: String) -> Effect[()] {
      Effect.pure(())
    }
  }

  case "x" {
    expect true
  }
}
