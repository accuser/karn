test commerce.payment {
  mocks Logger = First {
    fn log(msg: String) -> Effect[()] {
      Effect.pure(())
    }
  }

  mocks Logger = Second {
    fn log(msg: String) -> Effect[()] {
      Effect.pure(())
    }
  }

  test "x" {
    assert true
  }
}
