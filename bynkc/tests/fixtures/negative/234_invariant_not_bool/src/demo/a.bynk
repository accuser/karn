context demo.a

agent Counter {
  key id: String
  state {
    count: Int,
  }

  invariant bad:
    count

  on call bump() -> Effect[()] {
    commit { ...self.state, count: self.state.count + 1 }
    ()
  }
}
