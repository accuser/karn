context logging {
  exports capability { Log }

  capability Log {
    fn info(msg: String) -> Effect[()]
  }

  provides Log = SilentLog {
    fn info(msg: String) -> Effect[()] {
      ()
    }
  }
}
