context svc

service api from http {
  -- `Raw` expects a `Bytes` body first; a `String` there is the ordinary
  -- argument mismatch — no bespoke `Raw` error path.
  on GET("/x") by v: Visitor () -> Effect[HttpResult[()]] {
    Raw("not-bytes", "text/plain")
  }
}
