context demo

type UserId = String where NonEmpty

actor One { auth = Bearer(secret = "A"), identity = UserId }
actor Two { auth = Bearer(secret = "B"), identity = UserId }

service api from http {
  on GET("/x") by who: One | Two () -> Effect[HttpResult[String]] {
    Ok("x")
  }
}
