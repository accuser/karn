context demo.gauge

type Positive = Int where Positive

type Id = String where Matches("[a-z0-9]{1,8}")

agent Gauge {
	key id: Id

	state {
		level: Positive,
	}

	on call current() -> Effect[Positive] {
		self.state.level
	}
}
