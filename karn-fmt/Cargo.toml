[package]
name = "karn-fmt"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Formatter for the Karn DSL (used by karnc-lsp)."

[lib]
path = "src/lib.rs"

[dependencies]
karnc.workspace = true

[dev-dependencies]
