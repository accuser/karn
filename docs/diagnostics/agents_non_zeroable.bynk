context inventory

type Level = Int where Positive

agent Gauge {
  key id: String

  state {
    level: Level,        -- no initialiser, and Positive excludes 0
    retries: Int = 3,
  }

  on call peek() -> Effect[Result[(), String]] {
    Ok(())
  }
}
