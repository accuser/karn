# Program structure

How a Karn program is shaped: pure `commons` modules, `context` units, and the
`uses` / `consumes` / `exports` graph that wires them together.

**Understand**
- [How a Karn program is shaped](how-a-program-is-shaped.md)

**Do**
- [Consume another context's services](consume-services.md)

**See also:** [Spec §8 — Compilation model](../../spec/compilation-model.md),
[Reference — `karn.toml` manifest](../../reference/manifest.md).
